name: memoReal

deploy:
  - helm upgrade --install ${OKTETO_AUTODISCOVERY_RELEASE_NAME} backend/k8s/charts --set url="https://miso-memoReal-$(kubectl config view --minify -o jsonpath='{..namespace}').cloud.okteto.net"

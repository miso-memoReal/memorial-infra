apiVersion: v1
kind: Service
metadata:
  name: nginx
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP
  ports:
    - name: "http"
      port: 80
      targetPort: 80
  selector:
    memorial.service: nginx

apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "memorial-backend.fullname" . }}-test-connection"
  labels:
    {{- include "memorial-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "memorial-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

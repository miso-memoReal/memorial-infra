apiVersion: v1
kind: Service
metadata:
  name: {{ include "memorial-backend.fullname" . }}
  labels:
    {{- include "memorial-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "memorial-backend.selectorLabels" . | nindent 4 }}

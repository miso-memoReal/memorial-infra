apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: { { .Values.nginx.pvc.name } }
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: { { .Values.nginx.pvc.storage } }
